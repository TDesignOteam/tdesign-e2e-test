{
  "name": "tdesign-e2e",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "test": "jest --runInBand",
    "test:verbose": "jest --runInBand --verbose",
    "test:ci": "CI=true jest --runInBand --json --outputFile=test-results/results.json",
    "notify": "ts-node scripts/notify.ts",
    "test:debug": "node --inspect-brk node_modules/.bin/jest --runInBand"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "jest": "^29.7.0",
    "puppeteer": "^24.2.1",
    "ts-jest": "^29.2.5",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.3"
  },
  "author": "guowangyang",
  "license": "MIT",
  "description": "TDesign E2E testing (Puppeteer) - 防止白屏、防止元素丢失",
  "dependencies": {
    "t-comm": "^3.0.25"
  }
}